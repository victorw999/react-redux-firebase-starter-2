import React, { useState, useCallback } from "react";
import { useFirestore } from "react-redux-firebase";

const FirestoreComp = () => {
  const firestore = useFirestore();
  return;
};

export default FirestoreComp;
